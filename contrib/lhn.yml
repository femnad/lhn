archives:
  - url: https://vscode-update.azurewebsites.net/${version}/linux-x64/stable
    unless:
      cmd: code --version
      post: line 0
    version: 1.54.1
packages:
  common:
    - fish
  dnf:
    - vim-X11
settings:
  unpack_dir: ~/z/dy
